import React from 'react';
import { useParams } from 'react-router-dom';

export const TaeExample: React.FC = () => {
  const { id } = useParams();
  return (
    <div>
      <p>
				asdf
      </p>
    </div>
  );
};